<Page
    x:Class="WinUIClash.Views.LogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:models="using:WinUIClash.Models"
    mc:Ignorable="d">

    <Grid Padding="24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Grid RowSpacing="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Source={StaticResource S}, Path=LogsTitle_Text}" FontSize="20" FontWeight="SemiBold" />
            <AutoSuggestBox Grid.Row="1" Grid.Column="0" Height="32" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                            PlaceholderText="{Binding Source={StaticResource S}, Path=LogsSearchPlaceholder_PlaceholderText}"
                            QueryIcon="Find"
                            Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" />
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <ComboBox ItemsSource="{x:Bind ViewModel.LevelOptions}"
                          SelectedItem="{x:Bind ViewModel.SelectedLevel, Mode=TwoWay}"
                          Width="110" Height="32" FontSize="12" />
                <Button Command="{x:Bind ViewModel.TogglePauseCommand}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.PauseLabel, Mode=OneWay}"
                        Height="32">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <fluent:SymbolIcon Symbol="Pause" FontSize="12"
                                           Visibility="{x:Bind ViewModel.IsPaused, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                        <fluent:SymbolIcon Symbol="Play" FontSize="12"
                                           Visibility="{x:Bind ViewModel.IsPaused, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text="{x:Bind ViewModel.PauseLabel, Mode=OneWay}" FontSize="11" />
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.ExportCommand}"
                        ToolTipService.ToolTip="{Binding Source={StaticResource S}, Path=LogsExport_ToolTip}"
                        Height="32">
                    <fluent:SymbolIcon Symbol="Save" FontSize="14" />
                </Button>
                <Button Command="{x:Bind ViewModel.ClearCommand}" ToolTipService.ToolTip="{Binding Source={StaticResource S}, Path=CommonClear_ToolTip}" Height="32">
                    <fluent:SymbolIcon Symbol="Delete" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- 自动滚动开关 + 日志计数 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="4" Margin="4,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Source={StaticResource S}, Path=LogsAutoScroll_Text}" FontSize="11" VerticalAlignment="Center" />
                <ToggleSwitch MinWidth="0" OffContent="" OnContent=""
                              IsOn="{x:Bind ViewModel.AutoScroll, Mode=TwoWay}" />
            </StackPanel>
            <TextBlock Grid.Column="2" VerticalAlignment="Center"
                       FontSize="11" Opacity="0.5">
                <Run Text="{x:Bind ViewModel.LogCount, Mode=OneWay}" />
                <Run Text="{Binding Source={StaticResource S}, Path=LogsCountSuffix_Text}" />
            </TextBlock>
        </Grid>

        <!-- 日志列表 -->
        <ListView x:Name="LogList" Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.FilteredLogs, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <Grid Padding="4,6" ColumnSpacing="10" RightTapped="LogItem_RightTapped" DoubleTapped="LogItem_DoubleTapped" Tag="{x:Bind}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <!-- 时间戳 -->
                        <TextBlock Text="{x:Bind Timestamp, Converter={StaticResource DateTimeToTimeConverter}}"
                                   FontSize="11" Opacity="0.5" FontFamily="Consolas"
                                   VerticalAlignment="Center" Width="65" />
                        <!-- 级别标签 -->
                        <Border Grid.Column="1" CornerRadius="3" Padding="6,2"
                                VerticalAlignment="Center"
                                Tapped="LogLevelBadge_Tapped"
                                Tag="{x:Bind Level}">
                            <Border.Background>
                                <SolidColorBrush Color="{ThemeResource SubtleFillColorSecondary}" />
                            </Border.Background>
                            <TextBlock Text="{x:Bind Level}" FontSize="10"
                                       FontWeight="SemiBold"
                                       Foreground="{x:Bind Level, Converter={StaticResource LogLevelToColorConverter}}" />
                        </Border>
                        <!-- 内容 -->
                        <TextBlock Grid.Column="2" Text="{x:Bind Payload}" FontSize="12"
                                   FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 空状态 -->
        <StackPanel Grid.Row="2"
                    Visibility="{x:Bind ViewModel.FilteredLogs, Mode=OneWay, Converter={StaticResource EmptyCollectionToVisibilityConverter}}"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
            <fluent:SymbolIcon Symbol="DocumentBulletList" FontSize="32" Opacity="0.3" />
            <TextBlock Text="{Binding Source={StaticResource S}, Path=EmptyLogs_Text}" Opacity="0.5" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
