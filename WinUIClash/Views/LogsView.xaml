<Page
    x:Class="WinUIClash.Views.LogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:models="using:WinUIClash.Models"
    mc:Ignorable="d">

    <Grid Padding="24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="日志" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
            <AutoSuggestBox Grid.Column="1" Margin="16,0"
                            PlaceholderText="搜索日志…"
                            QueryIcon="Find"
                            Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <ComboBox SelectedItem="{x:Bind ViewModel.SelectedLevel, Mode=TwoWay}"
                          Width="100">
                    <x:String>全部</x:String>
                    <x:String>Debug</x:String>
                    <x:String>Info</x:String>
                    <x:String>Warning</x:String>
                    <x:String>Error</x:String>
                </ComboBox>
                <Button Command="{x:Bind ViewModel.ClearCommand}" ToolTipService.ToolTip="清空">
                    <fluent:SymbolIcon Symbol="Delete" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- 自动滚动开关 -->
        <ToggleSwitch Grid.Row="1" OnContent="自动滚动" OffContent="自动滚动"
                      IsOn="{x:Bind ViewModel.AutoScroll, Mode=TwoWay}" />

        <!-- 日志列表 -->
        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.FilteredLogs, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <Grid Padding="4,6" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <!-- 时间戳 -->
                        <TextBlock Text="{x:Bind Timestamp}" FontSize="11"
                                   Opacity="0.5" FontFamily="Consolas"
                                   VerticalAlignment="Center" Width="65" />
                        <!-- 级别标签 -->
                        <Border Grid.Column="1" CornerRadius="3" Padding="6,2"
                                VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{ThemeResource SubtleFillColorSecondary}" />
                            </Border.Background>
                            <TextBlock Text="{x:Bind Level}" FontSize="10"
                                       FontWeight="SemiBold"
                                       Foreground="{x:Bind Level, Converter={StaticResource LogLevelToColorConverter}}" />
                        </Border>
                        <!-- 内容 -->
                        <TextBlock Grid.Column="2" Text="{x:Bind Payload}" FontSize="12"
                                   FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
