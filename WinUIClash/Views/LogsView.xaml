<Page
    x:Class="WinUIClash.Views.LogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:models="using:WinUIClash.Models"
    mc:Ignorable="d">

    <Grid Padding="24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Uid="LogsTitle" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
            <AutoSuggestBox Grid.Column="1" Margin="16,0"
                            x:Uid="LogsSearchPlaceholder"
                            QueryIcon="Find"
                            Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <ComboBox ItemsSource="{x:Bind ViewModel.LevelOptions}"
                          SelectedItem="{x:Bind ViewModel.SelectedLevel, Mode=TwoWay}"
                          Width="110" FontSize="12" />
                <Button Command="{x:Bind ViewModel.TogglePauseCommand}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.PauseLabel, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <fluent:SymbolIcon Symbol="Pause" FontSize="12"
                                           Visibility="{x:Bind ViewModel.IsPaused, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                        <fluent:SymbolIcon Symbol="Play" FontSize="12"
                                           Visibility="{x:Bind ViewModel.IsPaused, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text="{x:Bind ViewModel.PauseLabel, Mode=OneWay}" FontSize="11" />
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.ExportCommand}"
                        x:Uid="LogsExport">
                    <fluent:SymbolIcon Symbol="Save" FontSize="14" />
                </Button>
                <Button Command="{x:Bind ViewModel.ClearCommand}" x:Uid="CommonClear">
                    <fluent:SymbolIcon Symbol="Delete" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- 自动滚动开关 + 日志计数 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ToggleSwitch x:Uid="LogsAutoScroll"
                          IsOn="{x:Bind ViewModel.AutoScroll, Mode=TwoWay}" />
            <TextBlock Grid.Column="2" VerticalAlignment="Center"
                       FontSize="11" Opacity="0.5">
                <Run Text="{x:Bind ViewModel.LogCount, Mode=OneWay}" />
                <Run x:Uid="LogsCountSuffix" />
            </TextBlock>
        </Grid>

        <!-- 日志列表 -->
        <ListView x:Name="LogList" Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.FilteredLogs, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <Grid Padding="4,6" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <!-- 时间戳 -->
                        <TextBlock Text="{x:Bind Timestamp, Converter={StaticResource DateTimeToTimeConverter}}"
                                   FontSize="11" Opacity="0.5" FontFamily="Consolas"
                                   VerticalAlignment="Center" Width="65" />
                        <!-- 级别标签 -->
                        <Border Grid.Column="1" CornerRadius="3" Padding="6,2"
                                VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{ThemeResource SubtleFillColorSecondary}" />
                            </Border.Background>
                            <TextBlock Text="{x:Bind Level}" FontSize="10"
                                       FontWeight="SemiBold"
                                       Foreground="{x:Bind Level, Converter={StaticResource LogLevelToColorConverter}}" />
                        </Border>
                        <!-- 内容 -->
                        <TextBlock Grid.Column="2" Text="{x:Bind Payload}" FontSize="12"
                                   FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
