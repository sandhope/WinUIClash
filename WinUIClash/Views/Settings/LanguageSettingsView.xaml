<UserControl
    x:Class="WinUIClash.Views.Settings.LanguageSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Padding="0,0,16,24">
            <!-- 语言选择 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel Spacing="4">
                    <RadioButton Content="简体中文" GroupName="Language"
                                 IsChecked="{x:Bind ViewModel.IsChinese, Mode=TwoWay}" />
                    <RadioButton Content="English" GroupName="Language"
                                 IsChecked="{x:Bind ViewModel.IsEnglish, Mode=TwoWay}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
