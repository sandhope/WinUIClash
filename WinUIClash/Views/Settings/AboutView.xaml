<UserControl
    x:Class="WinUIClash.Views.Settings.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Padding="0,0,16,24">
            <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutTitle_Text}" FontSize="20" FontWeight="SemiBold" />

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel Spacing="12" HorizontalAlignment="Center" Padding="24">
                    <fluent:SymbolIcon Symbol="Shield" FontSize="48" Foreground="{ThemeResource AccentFillColorDefaultBrush}" />
                    <TextBlock Text="WinUIClash" FontSize="20" FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutVersion_Text}" FontSize="12" Opacity="0.5"
                               x:Name="VersionText"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutDescription_Text}"
                               FontSize="12" Opacity="0.6" HorizontalAlignment="Center"
                               TextAlignment="Center" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutTechStack_Text}" FontSize="14" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="12" RowSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutFramework_Text}" Opacity="0.5" FontSize="12" />
                        <TextBlock Grid.Column="1" Text=".NET 10 + Windows App SDK 2.2" FontSize="12" />
                        <TextBlock Grid.Row="1" Text="UI" Opacity="0.5" FontSize="12" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="WinUI 3 (XAML) + Mica" FontSize="12" />
                        <TextBlock Grid.Row="2" Text="MVVM" Opacity="0.5" FontSize="12" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="CommunityToolkit.Mvvm 8.4" FontSize="12" />
                        <TextBlock Grid.Row="3" Text="{Binding Source={StaticResource S}, Path=AboutCore_Text}" Opacity="0.5" FontSize="12" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="ClashMeta (Mihomo)" FontSize="12" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel Spacing="4">
                    <HyperlinkButton Content="{Binding Source={StaticResource S}, Path=AboutFlClash_Content}" NavigateUri="https://github.com/chen08209/FlClash" />
                    <HyperlinkButton Content="{Binding Source={StaticResource S}, Path=AboutMihomo_Content}" NavigateUri="https://github.com/MetaCubeX/mihomo" />
                    <HyperlinkButton Content="{Binding Source={StaticResource S}, Path=AboutWinAppSdk_Content}" NavigateUri="https://learn.microsoft.com/windows/apps/winui/" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutActions_Text}" FontSize="14" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Click="OpenDataFolder_Click">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <fluent:SymbolIcon Symbol="FolderOpen" FontSize="14" />
                                <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutOpenDataFolderText_Text}" FontSize="12" />
                            </StackPanel>
                        </Button>
                        <Button Click="CopyVersion_Click">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <fluent:SymbolIcon Symbol="Copy" FontSize="14" />
                                <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutCopyVersionText_Text}" FontSize="12" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="CheckUpdateBtn" Click="CheckUpdate_Click">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Grid Width="14" Height="14">
                                    <fluent:SymbolIcon x:Name="UpdateIcon" Symbol="ArrowSync" FontSize="14" />
                                    <ProgressRing x:Name="UpdateProgressRing" IsActive="False"
                                                  Width="14" Height="14" Visibility="Collapsed" />
                                </Grid>
                                <TextBlock x:Name="CheckUpdateLabel" Text="{Binding Source={StaticResource S}, Path=AboutCheckUpdateText_Text}" FontSize="12" />
                            </StackPanel>
                        </Button>
                        <Button Click="ExportLogs_Click">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <fluent:SymbolIcon Symbol="ArrowExport" FontSize="14" />
                                <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutExportLogsText_Text}" FontSize="12" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <TextBlock x:Name="UpdateStatusText" FontSize="11" Opacity="0.6"
                               Visibility="Collapsed" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <TextBlock Text="{Binding Source={StaticResource S}, Path=AboutLicense_Text}" FontSize="11" Opacity="0.4" TextWrapping="Wrap" />
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>