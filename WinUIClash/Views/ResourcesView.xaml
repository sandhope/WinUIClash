<Page
    x:Class="WinUIClash.Views.ResourcesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:models="using:WinUIClash.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Source={StaticResource S}, Path=ResTitle_Text}" FontSize="20" FontWeight="SemiBold" Margin="24,16,24,0" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,16,24,0">
            <StackPanel Spacing="20">
                <!-- Geo 选项 -->
                <StackPanel Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=GeoOptions_Text}" FontSize="14" FontWeight="SemiBold" Opacity="0.8" />
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="16">
                        <StackPanel Spacing="12">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Source={StaticResource S}, Path=GeoAutoUpdate_Text}" VerticalAlignment="Center" />
                                <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.GeoAutoUpdate, Mode=TwoWay}"
                                               OnContent="" OffContent="" MinWidth="0" />
                            </Grid>
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Source={StaticResource S}, Path=GeoAutoUpdateInterval_Text}" VerticalAlignment="Center" />
                                <NumberBox Grid.Column="1" Value="{x:Bind ViewModel.GeoUpdateInterval, Mode=TwoWay}"
                                           Minimum="1" Maximum="8760" Width="120" SpinButtonPlacementMode="Inline" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Geo 资源 -->
                <StackPanel Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource S}, Path=GeoResources_Text}" FontSize="14" FontWeight="SemiBold" Opacity="0.8" />
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="models:GeoResourceItem">
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="0,0,0,8">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Spacing="4" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind DisplayName}" FontSize="15" FontWeight="SemiBold" />
                                            <TextBlock FontSize="11" Opacity="0.5" Text="{x:Bind FileInfoText, Mode=OneWay}" />
                                            <TextBlock FontSize="11" Opacity="0.4"
                                                       Text="{x:Bind Url, Mode=OneWay}"
                                                       TextTrimming="CharacterEllipsis" MaxWidth="520" />
                                        </StackPanel>
                                        <Button Grid.Column="1" Padding="10,6" VerticalAlignment="Center"
                                                Click="EditUrl_Click" Tag="{x:Bind}">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <fluent:SymbolIcon Symbol="Edit" FontSize="14" />
                                                <TextBlock Text="{Binding Source={StaticResource S}, Path=GeoEditUrl_Text}" FontSize="12" />
                                            </StackPanel>
                                        </Button>
                                        <Button Grid.Column="2" Padding="10,6" VerticalAlignment="Center"
                                                Click="Sync_Click" Tag="{x:Bind}"
                                                Visibility="{x:Bind IsUpdating, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <fluent:SymbolIcon Symbol="ArrowSync" FontSize="14" />
                                                <TextBlock Text="{Binding Source={StaticResource S}, Path=GeoSync_Text}" FontSize="12" />
                                            </StackPanel>
                                        </Button>
                                        <ProgressRing Grid.Column="2" Width="22" Height="22"
                                                      IsActive="{x:Bind IsUpdating, Mode=OneWay}"
                                                      Visibility="{x:Bind IsUpdating, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                      VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <ProgressRing Grid.Row="1"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Page>
