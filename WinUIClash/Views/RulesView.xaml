<Page
    x:Class="WinUIClash.Views.RulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:models="using:WinUIClash.Models"
    mc:Ignorable="d">

    <Grid Padding="24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="规则" FontSize="20" FontWeight="SemiBold" />
                <TextBlock FontSize="12" Opacity="0.5" VerticalAlignment="Bottom"
                           Text="{x:Bind ViewModel.TotalCount, Mode=OneWay}" />
            </StackPanel>
            <TextBox Grid.Column="2" Width="200"
                     PlaceholderText="搜索规则..."
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <!-- 表头 -->
        <Grid Grid.Row="1" Padding="12,8"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="8,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="类型" FontSize="12" FontWeight="SemiBold" Opacity="0.6" />
            <TextBlock Grid.Column="1" Text="匹配内容" FontSize="12" FontWeight="SemiBold" Opacity="0.6" />
            <TextBlock Grid.Column="2" Text="目标" FontSize="12" FontWeight="SemiBold" Opacity="0.6" />
        </Grid>

        <!-- 规则列表 -->
        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.FilteredRules, Mode=OneWay}"
                  SelectionMode="None"
                  CornerRadius="0,0,8,8">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Rule">
                    <Grid Padding="12,8" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <!-- 类型标签 -->
                        <Border CornerRadius="4" Padding="6,2"
                                HorizontalAlignment="Left"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <TextBlock Text="{x:Bind Type}" FontSize="11"
                                       FontFamily="Consolas" FontWeight="SemiBold" />
                        </Border>
                        <!-- 内容 -->
                        <TextBlock Grid.Column="1" Text="{x:Bind Payload}"
                                   FontSize="12" FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" Opacity="0.8" />
                        <!-- 目标代理 -->
                        <TextBlock Grid.Column="2" Text="{x:Bind Proxy}"
                                   FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 加载指示 -->
        <ProgressRing Grid.Row="2"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Page>
